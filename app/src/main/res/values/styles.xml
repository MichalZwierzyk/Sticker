<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="android:buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="NoTitleBarTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
    </style>

    <style name="MainActivityButtonStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">0.5</item>
        <item name="android:background">@drawable/main_activity_button_selector</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="StickersPaletteStyle">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:src">@mipmap/ic_launcher</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="Crop"></style>

    <style name="Crop.DoneCancelBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/crop_bar_height</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:divider">@drawable/crop_divider</item>
        <item name="android:showDividers" tools:ignore="NewApi">middle</item>
        <item name="android:dividerPadding" tools:ignore="NewApi">12dp</item>
        <item name="android:background">@color/crop_button_bar</item>
    </style>

    <style name="Crop.ActionButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/crop_selectable_background</item>
    </style>

    <style name="Crop.ActionButtonText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingRight">20dp</item> <!-- Offsets left drawable -->
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColor">@color/crop_button_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="Crop.ActionButtonText.Save">
        <item name="android:drawableLeft">@drawable/crop_ic_done</item>
        <item name="android:text">@string/crop_save</item>
    </style>

    <style name="Crop.ActionButtonText.Done">
        <item name="android:drawableLeft">@drawable/crop_ic_done</item>
        <item name="android:text">@string/crop_done</item>
    </style>

    <style name="Crop.ActionButtonText.Cancel">
        <item name="android:drawableLeft">@drawable/crop_ic_cancel</item>
        <item name="android:text">@string/crop_cancel</item>
    </style>

    <style name="Crop.ActionButtonText.OriginalSize">
        <item name="android:text">@string/crop_original_size</item>
        <item name="android:paddingRight">0dp</item>
    </style>

    <style name="Crop.ActionButtonText.SquareSize">
        <item name="android:text">@string/crop_square_size</item>
        <item name="android:paddingRight">0dp</item>
    </style>

    <style name="Crop.ActionButtonText.VerticalSize">
        <item name="android:text">@string/crop_vertical_size</item>
        <item name="android:paddingRight">0dp</item>
    </style>

    <style name="Crop.ActionButtonText.HorizontalSize">
        <item name="android:text">@string/crop_horizontal_size</item>
        <item name="android:paddingRight">0dp</item>
    </style>

</resources>
